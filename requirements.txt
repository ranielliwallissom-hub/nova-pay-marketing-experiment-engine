# Core pipeline (python/01-06, sql verification)
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.8

# SQL verification only (running sql/*.sql against the CSVs) -- not needed
# to run the core Python pipeline
duckdb==1.5.5

# LLM reviewer layer (llm/) -- only needed to run run_reviewer.py against
# the live API; schema validation and test-case files work without it
anthropic
jsonschema==4.26.0
